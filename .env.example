# Neon pooled connection string (required when switching from demo data to the database)
DATABASE_URL="postgresql://USER:PASSWORD@ep-example-123456.ap-southeast-1.aws.neon.tech/ziggy?sslmode=require"

# Vercel Cron uses this to authorize scheduled reminder requests
CRON_SECRET="replace-with-a-long-random-secret"

# Optional: Google OAuth and Drive API integration for a later connected mode
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"
